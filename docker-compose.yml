version: '3.8'

services:
  pythondemo:
    build:
      context: .
    container_name: pythondemo
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - data:/app/data
    working_dir: /app

volumes:
  data:
